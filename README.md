**Available languages:**
- 🇷🇺 [Русский](README.md)
- 🇺🇸 [English](README_EN.md)


# 📚 Лабораторная работа №3:
Включает в себя реализацию многопоточной системы серверного взаимодействия в системе из
[лабораторной работы №1](https://github.com/alkasadist/Java_lab1_Patterns)

## Задание:
Необходимо доработать реализованную в ЛР1 систему, используя полученные знания о Concurrency. \
Необходимо решить такую проблему многопоточного программирования, как
**клиент-серверное взаимодействие**:
- Организовать работу генераторов запросов, очереди запросов и исполнителей данных запросов. 
- Запросы должны обрабатываться в многопоточном режиме. Необходимо избежать гонок данных и всех
следующих из них проблем.

## 📖 Объяснение:
### Был добавлен новый класс ```Request```
Он содержит в себе всю необходимую информацию для успешной обработки запроса:
1) Информационные поля ```type```, ```fromPhone```, ```toNumber```.
2) Флаг успешного выполнения запроса ```success```.
3) Счётчик для синхронизации потоков ```done```.

### Был перепроектирован класс ```PhoneCallMediator```
Всё клиентское взаимодействие с ```PhoneCallMediator``` теперь происходит через метод ```submitRequest```.
Он помещает запрос в очередь запросов ```requestQueue```, которую постоянно считывают серверные
потоки (в методе ```processRequests```). Эти потоки забирают запрос из очереди и обрабатывают его.

Вся валидация звонков перенесена в ```PhoneCallMediator```, так как до этого часть проверок, 
исполняемых локально в ```PhoneProxy``` могли стать неактуальными за время обработки запроса сервером
ввиду их неатомарности.

### Добавлен генератор запросов в ```Main```
С помощью переменных ```phoneCount``` и ```requestCount``` можно регулировать количество активных 
телефонов и запросов, применяемых к каждому (каждый запрос - в отдельном потоке).
